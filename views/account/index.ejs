<% if (title) { %>
  <h1><%= title %></h1>
<% } else { res.redirect('/') } %>

<%- messages() %>

<h2>Welcome, <%= locals.accountData.account_firstname %></h2>

<a class="management-btn" href="/account/update">Update Account</a>

<% if (locals.accountData.account_type !== 'Client' ) { %>
  <h3>Inventory Management</h3>
  <p>
    <a class="management-btn" href="/inv">Go to Inventory Management</a>
  </p>
<% } %>
